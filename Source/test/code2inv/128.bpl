procedure main() {
  // variable declarations
  var x:int;
  var y:int;
  // pre-conditions
  x := 1;
  // loop body
  while (((x < y))) {
    x := (x + x);
  }
  // post-condition
assert( (x >= 1) );
}
