procedure main() {
    var x: int, y: int;
    x := 1;
    y := 0;
    while (y < 1000) {
        if (*) {
            break;
        }
        x := x + y;
        y := y + 1;
    }
    assert(x >= y);
    
}
