

procedure fib43()
{

	var i: int;
	var t: int;
	var x: int;
	var y: int;

	assume(x != y);
	assume(i == 0);
	assume(t == y);

  //call u := unknown();
	while(*){
		if(x > 0){
			y := y + x;
		}
    
	}

	assert(y >= t);	
}