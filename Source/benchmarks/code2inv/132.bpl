

procedure main() {
    var i:int; 
    var j:int;
    var c:int;
    var t:int;
    
    i := 0;
    while(*) {
        if(c > 48) {
            if (c < 57) {
                j := i + i;
                t := c - 48;
                i := j + t;
            }
        }
        
    } 
    assert (i >= 0);
}
