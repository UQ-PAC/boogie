

procedure main()
{
    var x:int; 
    var m:int; 
    var n:int;
    
    x := 0;
    m := 0;

    while (x < n) {
        
        if (*) {
            m := x;
        }
        x := x + 1;
    }

       assert ((n > 0) ==> m < n);
       //assert (m >= 0);
}
