

procedure main()
{
    var x:int; 
    var m:int;
    var n:int;
    
    m := 0;
    x := 0;

    while (x < n) {
        
        if (*) {
            m := x;
        }
        x := x + 1;
    }

       //assert (m < n);
       assert ((n > 0) ==> m >= 0);
}
