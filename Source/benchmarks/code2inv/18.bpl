

procedure main()
{
    var x:int; 
    var m:int; 
    var n:int;
    
    x := 1;
    m := 1;
    
    while (x < n) {
        
        if (*) {
            m := x;
        }
        x := x + 1;
    }

       //assert (m < n);
       assert ((n > 1) ==> m >= 1);
}
